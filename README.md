# wifi-logger-project
The WiFi Logger Project is a software-based system that monitors nearby WiFi networks using programming without IoT hardware. It records SSID, signal strength, and timestamps, stores logs for analysis, and uses a Telegram Bot to provide real-time updates and remote access through simple commands.
